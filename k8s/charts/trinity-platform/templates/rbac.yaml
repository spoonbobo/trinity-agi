# ─── OpenShell Bridge ServiceAccount ───────────────────────────────────────
# No RBAC Role needed — openshell-bridge does not access the K8s API.
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: openshell-bridge
  namespace: {{ .Values.global.namespace }}
automountServiceAccountToken: false
